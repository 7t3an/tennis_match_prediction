# 🎾 ЛОГІКА РОБОТИ СИСТЕМИ ПРОГНОЗУВАННЯ

## 📋 ЯК ЦЕ ПРАЦЮЄ (покрокова логіка):

### 1️⃣ **КОРИСТУВАЧ ВВОДИТЬ:**
```
✅ Ім'я гравця 1: "Carlos Alcaraz"
✅ Ім'я гравця 2: "Jack Draper"  
⚙️ (Опціонально) Покриття: "Hard"
⚙️ (Опціонально) Турнір: "ATP 500"
```

**ВСЕ!** Більше користувач нічого не вводить.

---

### 2️⃣ **ПРОГРАМА ЙДЕ В БАЗУ ДАНИХ:**

База даних: `data/processed/test_features.csv` (2025 рік - найсвіжіші дані)

**Для кожного гравця витягує ОСТАННІЙ матч:**

```python
# Приклад для Carlos Alcaraz:
carlos_matches = db[db['p1_name'] == 'Carlos Alcaraz' OR db['p2_name'] == 'Carlos Alcaraz']
latest_match = carlos_matches.iloc[-1]  # Останній рядок = найновіший матч
```

---

### 3️⃣ **З ОСТАННЬОГО МАТЧУ ГРАВЦЯ ВИТЯГУЄ ВСІ ДАНІ:**

#### **Базові дані гравця:**
- ✅ `p1_rank` - Ранг ATP (наприклад: 1)
- ✅ `p1_rank_points` - Рейтингові очки (наприклад: 11050)
- ✅ `p1_age` - Вік (наприклад: 21)
- ✅ `p1_hand` - Рука (R/L/U)
- ✅ `p1_ht` - Зріст (см)
- ✅ `p1_ioc` - Країна (ESP, GBR...)
- ✅ `p1_seed` - Сід в турнірі (якщо є)
- ✅ `p1_entry` - Тип входу (WC, Q, LL...)

#### **Rolling статистика (форма за останні 10 матчів):**
- ✅ `p1_ace_roll10` - Середня к-ть ейсів
- ✅ `p1_df_roll10` - Середня к-ть подвійних помилок
- ✅ `p1_1stIn_roll10` - % першого serve in
- ✅ `p1_1stWon_roll10` - % виграних на першому serve
- ✅ `p1_2ndWon_roll10` - % виграних на другому serve
- ✅ `p1_bpSaved_roll10` - % врятованих break points
- ✅ І т.д. (9 статистик total)

**ТЕ САМЕ для другого гравця!**

---

### 4️⃣ **ОБЧИСЛЮЄ HEAD-TO-HEAD ІСТОРІЮ:**

```python
h2h_matches = db[
    (db['p1_name'] == 'Carlos Alcaraz' AND db['p2_name'] == 'Jack Draper') OR
    (db['p1_name'] == 'Jack Draper' AND db['p2_name'] == 'Carlos Alcaraz')
]

# Рахує перемоги кожного:
h2h_p1_wins = 5  # наприклад
h2h_p2_wins = 2
h2h_total = 7
h2h_p1_win_rate = 5/7 = 71.4%
```

---

### 5️⃣ **ОБЧИСЛЮЄ ДОДАТКОВІ FEATURES:**

```python
# Різниця рангів
rank_diff = p1_rank - p2_rank  # 1 - 5 = -4 (НЕГАТИВНЕ = P1 КРАЩИЙ!)

# Різниця очок
rank_points_diff = p1_points - p2_points  # 11050 - 4440 = +6610 (ПОЗИТИВНЕ = P1 КРАЩИЙ!)

# Співвідношення рангів
rank_ratio = p2_rank / p1_rank  # 5 / 1 = 5.0 (>1 = P1 КРАЩИЙ!)

# Хто фаворит
is_p1_favorite = (p1_rank < p2_rank)  # 1 < 5 = True (P1 ФАВОРИТ!)
```

---

### 6️⃣ **СТВОРЮЄ DATAFRAME З УСІМА 53 FEATURES:**

```python
features = {
    'surface': 0,  # Hard=0, Clay=1, Grass=2, Carpet=3
    'draw_size': 128,
    'tourney_level': 2,  # G=0, M=1, A=2, D=3, F=4
    'indoor': 0,
    
    # P1 features (20 штук)
    'p1_seed': 1,
    'p1_entry': 0,
    'p1_hand': 0,  # R=0, L=1
    'p1_ht': 183,
    'p1_ioc': 'ESP',
    'p1_age': 21,
    'p1_rank': 1,
    'p1_rank_points': 11050,
    ... (rolling stats) ...
    
    # P2 features (20 штук)
    'p2_seed': 5,
    'p2_rank': 5,
    ... і т.д. ...
    
    # Seed features
    'is_p1_seeded': True,
    'is_p2_seeded': True,
    'seed_diff': -4,
    'p1_seed_tier': 1,
    'p2_seed_tier': 1,
    
    # H2H features
    'h2h_p1_wins': 5,
    'h2h_p2_wins': 2,
    'h2h_total_matches': 7,
    'h2h_p1_win_rate': 0.714,
    
    # Encoded features
    'tourney_level_encoded': 2,
    'surface_encoded': 0,
    
    # Rank features
    'rank_diff': -4,  # 🔥 ГОЛОВНИЙ СИГНАЛ!
    'rank_ratio': 5.0,  # 🔥 ГОЛОВНИЙ СИГНАЛ!
    'rank_points_diff': 6610,  # 🔥 ГОЛОВНИЙ СИГНАЛ!
    'is_p1_favorite': True,  # 🔥 ГОЛОВНИЙ СИГНАЛ!
}
```

---

### 7️⃣ **ПЕРЕДАЄ В ML МОДЕЛЬ:**

```python
model = load('xgboost_calibrated_model.pkl')

# Модель очікує DataFrame з 53 колонками В ПРАВИЛЬНОМУ ПОРЯДКУ
input_df = pd.DataFrame([features])
input_df = input_df[feature_cols]  # Переставляє колонки в правильний порядок

# Прогноз
prob_p1_wins = model.predict_proba(input_df)[0, 1]
# Результат: 0.75 (75% шанс що P1 виграє)
```

---

### 8️⃣ **МОДЕЛЬ АНАЛІЗУЄ:**

**Найважливіші features (Feature Importance):**

1. **seed_diff** (19.97%) - Різниця сідів
2. **rank_ratio** (16.44%) - Співвідношення рангів 🔥
3. **is_p1_seeded** (5.5%) - Чи P1 сіяний
4. **rank_points_diff** (5.5%) - Різниця очок 🔥
5. **h2h_p1_win_rate** (1.4%) - H2H історія

**Модель бачить:**
- Carlos Alcaraz: Ранг #1, 11050 очок
- Jack Draper: Ранг #5, 4440 очок
- rank_ratio = 5.0 (дуже великий сигнал що P1 сильніший!)
- rank_points_diff = +6610 (величезна різниця!)
- H2H: Alcaraz виграв 5 з 7 матчів

**Висновок моделі:** 
```
Carlos Alcaraz виграє з ймовірністю 75%
Jack Draper виграє з ймовірністю 25%
```

---

### 9️⃣ **ВІДОБРАЖАЄ РЕЗУЛЬТАТ:**

```
🎯 ПРОГНОЗ МОДЕЛІ

👤 Carlos Alcaraz
   75% шанс на перемогу
   Ранг: 1 | Очки: 11050

👤 Jack Draper
   25% шанс на перемогу
   Ранг: 5 | Очки: 4440

🔥 Модель дуже впевнена у перемозі
   Carlos Alcaraz
   з ймовірністю 75%

📈 Деталі:
   Різниця рангів: 4 (P1 вище)
   H2H матчі: 7 (P1: 71%)
   Очікувана точність: 73.7%
```

---

## 🎯 КРИТИЧНО ВАЖЛИВІ МОМЕНТИ:

### ❌ **МОДЕЛЬ НЕ ВИКОРИСТОВУЄ:**
- ❌ Імена гравців (їх немає в features!)
- ❌ Player IDs (видалили через data leakage!)
- ❌ Результати поточного матчу (це було б leakage!)

### ✅ **МОДЕЛЬ ВИКОРИСТОВУЄ:**
- ✅ Ранги та очки (з ОСТАННЬОГО матчу кожного гравця)
- ✅ Rolling статистику (форма, не результат поточного матчу!)
- ✅ H2H історію (минулі зустрічі)
- ✅ Фізичні дані (вік, зріст, рука)
- ✅ Seed інформацію (якщо є)

---

## 🔍 ЧОМУ МОДЕЛЬ ПРАВИЛЬНА:

### Приклад: **Carlos Alcaraz (#1) vs Jack Draper (#5)**

**rank_diff = 1 - 5 = -4**
- ❓ Чому негативне?
- ✅ Бо rank 1 < rank 5, і це означає що P1 кращий!
- 📊 Модель навчена: негативне rank_diff = P1 фаворит

**rank_ratio = 5 / 1 = 5.0**
- ❓ Що означає 5.0?
- ✅ Ранг P2 в 5 разів гірший ніж P1
- 📊 Модель навчена: ratio > 1 = P1 фаворит

**rank_points_diff = 11050 - 4440 = +6610**
- ❓ Що означає +6610?
- ✅ P1 має на 6610 очок більше!
- 📊 Модель навчена: позитивне = P1 кращий

**is_p1_favorite = True**
- ❓ Як визначили?
- ✅ Бо p1_rank (1) < p2_rank (5)
- 📊 Модель отримує чіткий Boolean сигнал

---

## 📊 РЕЗУЛЬТАТ:

Модель **правильно** визначає що Carlos Alcaraz (#1) має **75% шанс** виграти у Jack Draper (#5).

**Це логічно і правильно!** ✅

---

## 🚀 ПЕРЕВАГИ ТАКОЇ СИСТЕМИ:

1. ✅ **Автоматизація** - користувач тільки вибирає імена
2. ✅ **Актуальні дані** - завжди беремо ОСТАННІЙ матч
3. ✅ **Всі 53 features** - модель отримує все що потрібно
4. ✅ **Немає data leakage** - не використовуємо результат поточного матчу
5. ✅ **Production-ready** - використовуємо TEST 2025 для актуальних даних

---

**Створено:** 7 грудня 2025  
**Модель:** XGBoost + Probability Calibration  
**Точність:** 68.86%  
**ROC-AUC:** 0.76
